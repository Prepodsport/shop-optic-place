# Django Unfold - Инструкции по проверке

## ✅ Массовые действия работают!

**Хорошие новости: Django-unfold ПОДДЕРЖИВАЕТ кастомные поля в action_form!**

Особенность unfold - панель действий появляется **только при выборе товаров через чекбоксы** (в отличие от стандартного Django admin, где она видна всегда).

### Как применить скидку к товарам:

1. **Выберите товары** в списке (чекбоксы слева)
2. Внизу появится панель **"Select actions"**
3. В этой панели вы увидите поля:
   - 📊 **Скидка, %** - введите процент (например: 15)
   - 💰 **Цена со скидкой** - или введите итоговую цену (например: 1990.00)
   - ✅ **Пометить как SALE** - чекбокс для флага is_sale
4. Выберите действие **"💰 Применить скидку"** в dropdown
5. Нажмите **"Go"**
6. Скидка применится ко всем выбранным товарам!

## Исправленные файлы

Все admin.py файлы были адаптированы под django-unfold:

### ✅ appointments/admin.py
- Исправлены поля модели (full_name вместо name, service_type вместо appointment_type и т.д.)
- Все поля соответствуют модели Appointment

### ✅ catalog/admin.py
- Добавлены кастомные поля формы для управления скидками
- Поля `discount_percent_input`, `discount_price_input`, `clear_discount` определены в ProductAdminForm

### ✅ Остальные файлы
- accounts/admin.py
- orders/admin.py
- content/admin.py
- pages/admin.py
- token_manager/admin.py
- integrations/admin.py

## Как проверить кастомные поля в ProductAdmin

1. Запустите сервер:
   ```bash
   cd backend
   python manage.py runserver
   ```

2. Откройте админку: `http://127.0.0.1:8000/admin/`

3. Перейдите в Catalog → Products

4. Откройте любой товар для редактирования

5. В разделе **"Цены"** должны быть видны поля:
   - ✅ **Price** (обычное поле модели)
   - ✅ **Old price** (обычное поле модели)
   - ✅ **Скидка** (readonly поле - вычисляемое)
   - ✅ **Скидка, %** (кастомное поле - `discount_percent_input`)
   - ✅ **Цена со скидкой** (кастомное поле - `discount_price_input`)
   - ✅ **Сбросить скидку** (кастомное поле - `clear_discount`)

6. В разделе **"Изображение"** должно быть:
   - ✅ Поле загрузки изображения
   - ✅ Красивое превью с тенью и скруглением

7. В разделе **"Генерация вариаций"** должна быть:
   - ✅ Кнопка "📦 Создать вариации" (если есть атрибуты)
   - ✅ Или сообщение с эмодзи о том, что нужно добавить атрибуты

## Работа с кастомными полями скидки

### Применить скидку в процентах:
1. Введите процент в поле "Скидка, %", например: `15`
2. Сохраните товар
3. Поле `old_price` автоматически заполнится текущей ценой
4. Поле `price` пересчитается с учетом скидки

### Применить скидку по цене:
1. Введите итоговую цену в поле "Цена со скидкой", например: `1999.00`
2. Сохраните товар
3. Поле `old_price` автоматически заполнится текущей ценой
4. Поле `price` станет равно введенной цене со скидкой

### Сбросить скидку:
1. Отметьте чекбокс "Сбросить скидку"
2. Сохраните товар
3. Цена вернется из `old_price` в `price`
4. Поле `old_price` очистится

## Особенности django-unfold

### Декоратор @display
Все кастомные методы отображения используют декоратор `@display`:
```python
@display(description="Название колонки", ordering="field_name")
def custom_method(self, obj):
    return "значение"
```

### Классы админки
- `ModelAdmin` → `unfold.admin.ModelAdmin`
- `TabularInline` → `unfold.admin.TabularInline`
- `StackedInline` → `unfold.admin.StackedInline`

### Форма
Кастомные поля формы должны быть определены **перед** `class Meta`:
```python
class MyForm(forms.ModelForm):
    custom_field = forms.CharField(...)  # ДО Meta

    class Meta:
        model = MyModel
        fields = "__all__"
```

## Возможные проблемы

### Кастомные поля не отображаются
1. Убедитесь, что поля определены в `ProductAdminForm` **перед** `class Meta`
2. Проверьте, что `form = ProductAdminForm` указан в `ProductAdmin`
3. Убедитесь, что поля есть в `fieldsets`

### Ошибки readonly полей
- Все readonly поля должны быть либо полями модели, либо методами класса admin
- Методы должны принимать параметр `obj`

### Стили не применяются
- Unfold использует TailwindCSS
- Избегайте встроенных стилей Django (`vTextField`, `vCheckboxLabel`)
- Используйте стандартные виджеты без дополнительных классов

## Debugging

Если что-то не работает:

1. Проверьте консоль браузера на ошибки JavaScript
2. Проверьте логи Django сервера
3. Убедитесь, что все миграции применены: `python manage.py migrate`
4. Очистите кеш браузера (Ctrl+Shift+R)
5. Проверьте настройки UNFOLD в `settings.py`
